
public int start1(int[] a, int[] b) {

int antal = 0;

  if (a.length != 0) {

     if (a[0]== 1){ 
       antal++;
     } 
  }

  if (b.length != 0) {

     if (b[0]== 1){ 
       antal++;
       
     } 
  
  }
     return antal;
}
